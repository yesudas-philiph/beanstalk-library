docker container for gitlab runner
prperly configured working eb-cli environment image
working ssh git setup should be in the docker image so that you can git clone it and then eb cli will upload it to the s3 and later site files will be deployed to app server
